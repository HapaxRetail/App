<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/hapax_nav"
    app:startDestination="@id/searchActivity">

    <activity
        android:id="@+id/searchActivity"
        android:name="hapax.app.FragmentStores"
        android:label="SearchActivity"
        tools:layout="@layout/fragment_stores" />

    <activity
        android:id="@+id/appActivity"
        android:name="hapax.app.FragmentProducts"
        android:label="AppActivity"
        tools:layout="@layout/fragment_products"/>
</navigation>