<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/hapax_nav"
    app:startDestination="@id/searchActivity">

    <activity
        android:id="@+id/searchActivity"
        android:name="hapax.app.SearchActivity"
        android:label="SearchActivity"
        tools:layout="@layout/search_layout" />

    <activity
        android:id="@+id/appActivity"
        android:name="hapax.app.AppActivity"
        android:label="AppActivity"
        tools:layout="@layout/app_layout"/>
</navigation>